mixin detail-1(data)
    .card(class= attributes.class)

        a.card__img-wrap(href= data.link)
            .card__img(style='background-image: url(' + data.img + ')')

        .card__text-wrap
            .card__title-wrap
                a.link.link_white.card__title(href= data.link)= data.title