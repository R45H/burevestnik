mixin pharos(data)
	.pharos

		.pharos__img-wrap
			.pharos__img(style='background-image: url(' + data.img + ');')

		.pharos__text-wrap
			h2.subtitle.main__space-bot-half= data.title
			.text
				ul

					each val in data.list
						li= val