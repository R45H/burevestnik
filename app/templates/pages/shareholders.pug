include ../mixins/bread

.section
	.container
		+bread(class= 'main__bread')

		h1.title.title_left.title_lg Акционерам

		.main__btn-wrap
			each val in shareholders
				.main__btn
					a.btn.btn_wide(href='#')= val