include ../mixins/bread
include ../mixins/card

.section.section_space-bot_none
	.container
		+bread(class= 'main__bread')
		h1.title= page.title

.section
	.container
		.row

			each val, i in card_catalog_2
				.col-sm-6.col-md-4.col-lg-3.main__space-col
					+card(val)

				- if (i == 2) break;